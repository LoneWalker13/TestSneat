@using Microsoft.AspNetCore.Mvc.Localization

@inject IViewLocalizer Localizer


@model BusinessCourse_Application.Services.Membership.Command.UpdateMembershipCommand

<form id="membershipForm" enctype="multipart/form-data">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel1">@Localizer.GetString("EditMembership")</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <input type="hidden" asp-for="Id" />
                <div class="row g-6 mb-4">
                    <div class="col mb-0">
                        <label class="form-label">@Localizer.GetString("Rank")</label>
                        <input type="text" id="membershipRank" class="form-control" asp-for="Rank">
                    </div>
                    <div class="col mb-0">
                        <label  class="form-label">@Localizer.GetString("Tier")</label>
                        <input type="number" id="membershipTier" class="form-control" asp-for="Tier">
                    </div>
                </div>
                <div class="row g-6">
                    <div class="col mb-0">
                        <label  class="form-label">@Localizer.GetString("Status")</label>
                        <div class="col mb-0">
                            <input type="radio" asp-for="Status" value="true" /> @Localizer.GetString("Active")
                            <input type="radio" asp-for="Status" value="false" /> @Localizer.GetString("Inactive")
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Close</button>
                <button id="submit" type="button" class="btn btn-primary">Save changes</button>
            </div>
        </div>
    </div>
</form>

<script>
    $("#submit").click(function () {
        var formValid = $('#membershipForm').valid();

        if (!formValid) return;
        // initToastr();
        showLoading();
        editRecord();
    });

    function editRecord() {
        var formData = new FormData($('#membershipForm')[0]);

        $.ajax({
            url: "/Memberships/EditMemberships",
            type: "POST",
            contentType: false,
            processData: false,
            data: formData,
            dataType: "json",
            success: function (data) {
                hideLoading();
                if (data.result.succeeded == true) {
                    toastr["success"]("The action has been completed successfully", "Success");
                    globalTableData.api().ajax.reload();
                    $('#membershipModal').modal('hide');
                } else {
                    toastr["error"](data.message, "Error");
                }
            },
            error: function (e) {
                hideLoading();
                $('#membershipModal').modal('hide');
                toastr["error"](e, "Error");
            }
        });
    }

    var showLoading = function () {
        $(".loading").show();
    }

    var hideLoading = function () {
        $(".loading").hide();
    }
</script>
